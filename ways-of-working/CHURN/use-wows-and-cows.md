When performing a user-requested action, consult relevant ways-of-working (wow) and context-wayfinding (cow) before proceeding.

wow tells you how to work. cow tells you where you are and what is already known.

Read applicable blocks from both before deciding how to act. Let them shape your approach rather than defaulting to generic behavior.
